[phases.setup]
nixPkgs = [
  "nodejs-18_x",
  "npm-9_x",
  "openssl"
]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "cd ..",
  "npx prisma generate",
  "cd webapp",
  "npm run build"
]

[start]
cmd = "npm run start"