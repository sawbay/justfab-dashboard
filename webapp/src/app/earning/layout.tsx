import { createMetadata } from "@/utils/metadata";

export const metadata = createMetadata("Earning");

export default function PredictionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
